export enum tagTypes {
  auth = "auth",
  user = "user",
  profile = "profile",
  users = "users",
  category = "category",
  service = "service",
  order = "order",
  portfolio = "portfolio",
  // pre
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.user,
  tagTypes.profile,
  tagTypes.users,
  tagTypes.category,
  tagTypes.service,
  tagTypes.order,
  tagTypes.portfolio,
  //pre
];
